// Jest setup for the Expo (SDK 55) app — React Native Testing Library.
// Add module mocks here as component tests need them (e.g. reanimated,
// expo-router, async-storage). Kept minimal on purpose; extend per test.
//
// Common ones we'll likely need once component tests start:
//   jest.mock('react-native-reanimated', () => require('react-native-reanimated/mock'));
// Add them when a test actually pulls the dependency in.
